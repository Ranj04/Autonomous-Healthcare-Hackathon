# /data — synthetic test data (provided at the event)

Drop the prepped synthetic files here before running Phase 2+ verify gates:

- `reference.json` — fair-price table keyed by CPT (`fair_price_usd.high`, etc.)
- `ground-truth.json` — expected parse + overcharge results for the three bills
- `discharge-ground-truth.json` — expected parse for `discharge1` (Phase 7)

> Synthetic data only. Never place real patient data (PHI) here.
